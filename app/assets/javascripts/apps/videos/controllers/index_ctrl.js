/**
 * @ngdoc controller
 * @name IndexCtrl
 *
 * @description Controller for the video index page 
 */
(function() {
  "use strict";

var IndexCtrl = function($scope) {
};

IndexCtrl.$inject = ['$scope'];

angular.module("VideoApp")
       .controller('IndexCtrl', IndexCtrl);
}());

