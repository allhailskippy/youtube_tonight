<div ng-app="PlayerApp" class="player_index">
  <ng-view></ng-view>
</div>
<style>
  html {
    width: 100%;
    height: 100%;
    /* stardust.png pattern from subtlepatterns.com */
    background:url(<%= asset_path("logo.png")%>) center center no-repeat, url('<%= asset_path("stardust.png")%>');
  }

  body {
    padding: 0;
  }

  /* Full size video playback */
  .full_window iframe {
    display: block;       /* iframes are inline by default */
    background: #000;
    border: none;         /* Reset default border */
    height: 100vh;        /* Viewport-relative units */
    width: 100vw;
  }
</style>
